<li id="<%= task.id %>">
  <%= check_box_tag :check_box_done, 1, task.status , data: { remote: true, url: task_path(task.id), params: {status: !task.status}.to_param, method: "put" }%>
  <%= task.title  %>
  <% unless task.deadline.nil? %>
    , to read: <%= task.deadline.strftime('%D')  %>
  <% end %>
  <%= link_to task_path(task), method: :delete, remote: true, data: { confirm: 'Are you sure you want to delete this task?' } do %>
    <i class="fa fa-trash" aria-hidden="true"></i>
  <% end %>


</li>